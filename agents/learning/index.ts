import type { AgentMessage } from "@adaptlearn/shared/types";

/**
 * Learning Agent — POC v1
 *
 * Selects what the user should see next based on their progress,
 * sequences the experience, and triggers assessment on lesson complete.
 *
 * Implementation: Session 7
 */
export async function handleMessage(_msg: AgentMessage): Promise<void> {
  throw new Error("Learning Agent not implemented yet — see Session 7");
}
