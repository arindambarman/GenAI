import type { AgentMessage } from "@adaptlearn/shared/types";

/**
 * Scout Agent — POC v1
 *
 * Researches job postings and market data for a given topic.
 * Produces a SkillMap with skills in demand and demand scores.
 *
 * Implementation: Session 4
 */
export async function handleMessage(_msg: AgentMessage): Promise<void> {
  throw new Error("Scout Agent not implemented yet — see Session 4");
}
