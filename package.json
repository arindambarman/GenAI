{
  "name": "adaptlearn-poc",
  "version": "0.1.0",
  "private": true,
  "description": "AdaptLearn — AI-powered adaptive learning platform for banking professionals",
  "scripts": {
    "dev": "pnpm --filter @adaptlearn/web dev",
    "build": "pnpm --filter @adaptlearn/web build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "postgres": "^3.4.8",
    "tsx": "^4.21.0",
    "vitest": "^4.0.18"
  }
}
